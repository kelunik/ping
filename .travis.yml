sudo: required

language: php

php:
  - 5.5
  - 5.6
  - 7.0

install:
  - composer install

before_script:
  - which php

script:
  - sudo $(php -r 'if (PHP_MAJOR_VERSION >= 7) echo "phpdbg -qrr"; else echo "php";') vendor/bin/phpunit --coverage-text --coverage-clover build/logs/clover.xml
